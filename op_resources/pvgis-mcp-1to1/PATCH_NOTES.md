# Patch notes

This package is intended to replace the broken `pvgis-mcp-1to1` folder that produced:

- `Cannot find name 'node:fs'`
- `Cannot find name 'process'`
- `Cannot find name 'Buffer'`
- `Cannot find module '@modelcontextprotocol/server/stdio'`
- `Property 'parameters' does not exist on type 'OpenApiParameter[] | OpenApiOperation'`

The replacement compiles under strict TypeScript.
